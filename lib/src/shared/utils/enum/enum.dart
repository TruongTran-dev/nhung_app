// ignore_for_file: constant_identifier_names

enum FrequencyType { daily, week, monthly, quarterly, yearly, weekday }

enum TransactionType { expense, income }

enum TransactionStatus { on_going, up_coming, finished }
